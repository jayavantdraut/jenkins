pipeline {
    agent any
    stages {
        stage('Example Build') {
            steps {
                 echo "jenkins pipline from code"
                 test()
            }
        }

    }
}

def test(){
echo "function call"
}